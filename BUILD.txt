RuleTrace — Build & Run (فارسی)
================================

دانلود ZIP فقط سورس دارد — پوشه bin بعد از Build ساخته می‌شود.

مرحله ۱ — Extract ZIP در:
  C:\Users\sadathoseini-sh\Downloads\ruletrace

مرحله ۲ — Build (یکی از روش‌ها):

  روش A — CMD:
    cd C:\Users\sadathoseini-sh\Downloads\ruletrace
    build.cmd "C:\Users\sadathoseini-sh\Desktop\dll new"

  روش B — PowerShell:
    cd C:\Users\sadathoseini-sh\Downloads\ruletrace
    $msbuild = & "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe" -latest -requires Microsoft.Component.MSBuild -find "MSBuild\**\Bin\MSBuild.exe" | Select-Object -First 1
    & $msbuild RuleTrace.sln /t:Rebuild /p:Configuration=Release /p:Platform="Any CPU" /p:DllPath="C:\Users\sadathoseini-sh\Desktop\dll new"

  روش C — Visual Studio:
    RuleTrace.sln → Release → Rebuild Solution

بعد از Build:
  C:\Users\sadathoseini-sh\Downloads\ruletrace\bin\RuleTrace.exe

مرحله ۳ — c:\dll10 (یک بار، PowerShell as Administrator):
    setup-dll10.cmd "C:\Users\sadathoseini-sh\Desktop\dll new"

مرحله ۴ — تست DB:
    cd bin
    RuleTrace.exe --test-db

مرحله ۵ — ساخت cache محلی (یک بار، ممکن است ۵–۲۰ دقیقه):
    RuleTrace.exe --clear-formula-cache --nidproc "FA77A442-29CD-4DDC-ADEA-A3D3A6183F28" --formula Solh --recompile

  cache ذخیره می‌شود در:
    C:\Users\sadathoseini-sh\Desktop\dll new\SafaFormulaCache\06065CA7-8B68-491F-A002-2AC9CAC8AE34\344\

مرحله ۶ — Run روزانه (سریع، بدون recompile):
    RuleTrace.exe --nidproc "FA77A442-29CD-4DDC-ADEA-A3D3A6183F28" --formula Solh --watch Calc_Chandganeh
