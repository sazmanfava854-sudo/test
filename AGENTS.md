# AGENTS.md

## Cursor Cloud specific instructions

This repository is currently empty (initialized with only a `README.md`). There is no application code, no build system, no dependencies, and no services to run.

### Current state
- Single file: `README.md` containing `# test`
- No package manager lockfiles, no language-specific project files
- No Docker configuration
- No CI/CD pipeline

### For future agents
- When application code is added, update this section with relevant build/run/test commands.
- Check for new files like `package.json`, `requirements.txt`, `go.mod`, `*.sln`, `Cargo.toml`, etc. to determine the technology stack.
- Update the VM environment setup script accordingly once dependencies exist.
